# Certs que o `tailscale cert` escreve no diretório atual.
# Nunca devem ir pro repo — o `tailscale serve` gerencia os dele internamente.
*.crt
*.key
*.pem

# Agents
.claude/

# Specs e plans do brainstorming/writing-plans: ficam locais, não sobem.
docs/superpowers/

# Logs e pids dos scripts de teste
*.log
*.pid

node_modules/
.DS_Store
